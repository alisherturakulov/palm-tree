const form = document.getElementById("uploadForm");
const image = document.getElementById("imageFile");
const messageArea = document.getElementById("messageArea");

let userMessage = `<div class="msg user"></div>`
let assistantMessage = `<div class="msg assistant"></div>`

form.addEventListener('submit', async function(e){
    e.preventDefault(); // stops form from clearing page

    // Need to fetch Python data

})
